import React from 'react'

const ConfirmDelete = (props)=>{
    console.log(props)
    return <div> Hello Delete</div>
    //onClick={() => { props.clickHandler(id) }}
}

export default ConfirmDelete